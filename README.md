Documentation is available online: http://code.google.com/p/nanoflann/

The library consists only of the headers in 'include/', but you can 
build examples and unit tests with CMake: 

$ mkdir build
$ cd build
$ cmake ..
$ make && make test

