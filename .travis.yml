language: cpp

compiler: 
  - gcc
  - clang  

before_install:
  - sudo apt-get update -d

install:
  - sudo apt-get install build-essential pkg-config cmake libgtest-dev

script:
  - bash .travis.sh

env:
  - TASK="build"
  - TASK="test"

